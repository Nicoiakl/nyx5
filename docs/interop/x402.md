# x402 ↔ Nyx5

**x402** revives the HTTP status nobody ever used. A server answers `402 Payment Required` saying
what the resource costs, the client comes back with proof of payment, and the server answers `200`
with a settlement receipt. It is stewarded by the x402 Foundation under the Linux Foundation.

**Checked against**: x402 v2 (`x402Version: 2`), repository `x402-foundation/x402`, `main` at commit
`fdeda56` of 2026-09-09. Verified 2026-09-09.

Nyx5 speaks this. `src/puentes/x402.js` builds and reads the wire, and `test/x402.test.js` proves
the price a house announces is the price its ledger actually charges.

## Why this is conformant without a blockchain

The obvious objection is that x402 is a crypto payment standard and Nyx5 has no chain. That is not
what the specification says.

The core specification encourages non-blockchain networks to use the CAIP-2 identifier format, and
the repository already contains a scheme binding that settles in fiat: `cloudflare:402` uses `USD`
as the asset, a role constant instead of a wallet address as `payTo`, Ed25519 HTTP message
signatures for authentication, an empty transaction hash, stateless verification, and no external
facilitator at all.

Every one of those choices matches how Nyx5 already works. The precedent is in their repository,
not in our reading of it.

## The wire

| x402 v2 | Nyx5 | Verdict |
|---|---|---|
| `402` with the `PAYMENT-REQUIRED` header | A mailbox with a stamp policy, answering with its price | **equivalent** |
| `PAYMENT-SIGNATURE` header from the client | A signed envelope carrying a `stamp` field | **partial** — same role, different container. Ours is the envelope the protocol already signs |
| `PAYMENT-RESPONSE` header on success | The ledger entry id for the stamp that was charged | **equivalent** |
| `SettlementResponse.transaction` | The ledger entry id | **partial** — not a chain hash. A party to the entry can retrieve it from the ledger, signed by the house, and see the balanced double entry. Anyone else cannot: the journal is not public |
| `GET /supported` on a facilitator | `GET /x402/supported`, self-hosted | **equivalent** — the specification allows a resource server to host these itself |
| `POST /verify` and `POST /settle` | none | **missing here** — a house settles against its own ledger and delegates to nobody |
| The Bazaar discovery extension | The federated index and the agent directory | **partial** — both catalogue resources, neither can read the other |

## The objects

| x402 field | Nyx5 | Verdict |
|---|---|---|
| `scheme` | `exact` | **equivalent** |
| `network` | `nyx5:1` | **partial** — see the note below |
| `amount` (string, atomic units) | The price in tokens, as a string | **equivalent** |
| `asset` | `NYX` | **partial** — x402 expects a contract address or an ISO 4217 code, and this is neither |
| `payTo` | `agent@domain` | **partial** — x402 expects an address or a role constant; ours is an address in a different sense |
| `maxTimeoutSeconds` | 60 | **equivalent** |
| `extra.paymentFlow` | `authorization` or `upfront` | **equivalent** |
| `resource.url`, `mimeType`, `description` | The mailbox URL and its description | **equivalent** |
| `extensions` | none | **missing here** |
| Payment expiry | none in `PaymentRequirements`; it lives in the scheme payload | **partial** — a Nyx5 envelope has `expires`, which is the same idea in a different place |

### Why the network is `nyx5:1` and not the house

CAIP-2 requires `namespace:reference`, where the reference matches `[-_a-zA-Z0-9]{1,32}`. A dot is
not legal there, so `nyx5:nyx5.com` is not a CAIP-2 identifier and a conformant client would
reject it. The network is therefore the protocol, and the house travels in `payTo`, which is
already `agent@domain`.

The cost of that choice is stated plainly: two different houses look like the same network on the
wire. A payment on one house is not valid on another, and only `payTo` says which is which.

## What Nyx5 has that x402 does not

| Nyx5 | Verdict |
|---|---|
| A verifier that decides whether the delivery was real | **missing there** — x402 settles a payment, it does not judge a delivery |
| Bonds: a false claim forfeits a deposit | **missing there** |
| Reputation as a public ledger query | **missing there** |
| Chained mandates with a spending cap per link | **missing there** — the `upto` scheme caps one payment, not a delegation chain |
| A store-and-forward mailbox | **missing there** — x402 is request and response, both parties online |

The `escrow` payment flow in x402 comes closest: settle, deliver the resource, settle again. But
x402 does not say who decides that the second settle should happen, and Nyx5 is exactly that
decision.

## The escrow survives without us holding anything

The obvious fear when moving to real money is that the escrow has to go. If a house cannot hold
third-party funds without becoming a regulated financial institution, and the escrow is the best
thing this protocol has, that looks like a forced trade.

It is not. The `auth-capture` scheme (`specs/schemes/auth-capture/`, checked 2026-09-09) already
does exactly what the Libro does, and the funds sit somewhere neither party controls.

| Nyx5 today | `auth-capture` | Verdict |
|---|---|---|
| Accepting an escrow quote holds the buyer's money | `authorize` reserves the funds | **equivalent** |
| `release` pays the seller when the check passes | `capture` | **equivalent** |
| `refund` returns the money when the check fails | `void` | **equivalent** |
| A contract that reaches its `deadline` unresolved | `reclaim`, the buyer's unilateral escape hatch after the capture deadline, never relayed through anyone | **missing here** — our deadline only schedules a notice, it gives the buyer no way to take the money back |
| The house account holds the escrowed balance | `AuthCaptureEscrow`, a singleton contract that holds funds and gates every operation on the operator | **partial** — same behaviour, but the money is in a contract instead of in our ledger |

The last row is the whole point. On this rail the money never enters an account a house controls,
so a house arbitrates without ever possessing what it arbitrates over. The buyer's protection does
not depend on the house being honest or solvent: the client signs a maximum, the fee is bounded,
and `reclaim` returns the funds unilaterally once the deadline passes.

`reclaim` is also a real gap on our side, independent of any of this. A Nyx5 escrow that reaches
its deadline with nobody deciding just sends a notice. The money stays held. That is a worse deal
for the buyer than the rail we are mapping to, and it is fixable in our own ledger today.

### The correction that matters: nothing implements it yet

The mapping above is real and the contract exists. What does not exist is anyone running it.
Checked on 2026-09-09 by asking five facilitators what they support, including the open one and
the largest mainnet ones: **not one advertises `auth-capture`**. They offer `exact`, `upto` and
`batch-settlement`. And in the reference repository the scheme ships **client code only**: there is
no server and no facilitator implementation.

So the honest statement is not "we can have the escrow for free on this rail". It is: the standard
describes our escrow almost exactly, the contract is deployed, and we would be building and
operating the operator side ourselves, first. That is a real cost and it belongs in the decision,
not in a footnote.

Worth watching: one facilitator advertises a scheme it calls `escrow`, which is not in the
specification. Unexamined here.

**The remaining design choice**, and it is not settled here: `extra.operatorType` decides who may
trigger the release. With `delegated` the facilitator relays what we ask and, in the spec's own
words, its verification is the only gate, so that is trust rather than proof. With `custom` the
operator is a contract, which is the only way our verdict gates the money provably rather than
cooperatively.

## What we claim, and what we do not

**We claim**: a Nyx5 house speaks the x402 v2 HTTP transport. It answers `402` with a well-formed
`PAYMENT-REQUIRED`, it answers `200` with a `PAYMENT-RESPONSE` whose transaction id resolves in the
ledger, and it declares its capabilities at `/x402/supported`.

**We do not claim**: that an arbitrary x402 client can pay a Nyx5 house today. It cannot. Our asset
is a house token, not a contract address or a currency code, so a generic client can read the price
and will not know what to pay with. That is the same position `cloudflare:402` is in, where a payer
must have an account first. Conformant on the wire, and not universally interoperable. Those are
different claims and we make only the first.

**We also do not claim** any affiliation with the x402 Foundation. There is no conformance program,
no certification, and no official registry of implementations. Anyone who tells you they are
"x402 certified" is describing something that does not exist.

## A payment with real money, on Ethereum mainnet

On 2026-09-10, Nyx5 charged one real US cent. Read from the chain, not from the facilitator's
own answer:

| | |
|---|---|
| Transaction | `0xfed9ce65b3bd843ddcf5b57221d202b214c9c3b694282135464608c0e40a2021` |
| Status | success, block 25948376 |
| Transfer | US$0.01 of USDC, payer to payee, one hop |
| Sent by | the facilitator's address, not ours |
| Gas | 103,389 at 1.13 gwei, about US$0.29, **paid by the facilitator** |
| Payer's native balance | 0 wei, before and after |

The economics are worth stating plainly, because they cut both ways. The facilitator spent roughly
twenty-nine times the value of the payment to settle it, and absorbed that cost. On this chain a
one-cent payment only works because someone else is subsidising it. On a cheap chain the same
settlement costs a fraction of a cent. **The protocol works either way; the choice of chain is
what decides whether the economics do.**

One lesson that cost two attempts, and it is a design lesson rather than a bug. An EIP-3009
authorization carries a validity window, and a pre-signed payment handed over to be settled later
expires. In the real flow this never happens, because the agent signs at the moment it pays. Any
tooling that separates signing from settling has to sign at the last moment, not the first.

## A real payment, on a real chain

On 2026-09-09, `demo/x402-testnet.mjs` ran the full resource-server role against the open
facilitator on Base Sepolia and settled a real transfer. Read from the chain, not from the
facilitator's own answer:

| | |
|---|---|
| Transaction | `0xb16846aa84b0a287197f8148d7d01db4f7dbe6cbd054af3d727d6eef7d72ee20` |
| Status | success, block 46606837 |
| Transfer | US$0.25 from the payer to the payee, one hop |
| Sent by | the facilitator's own address, not ours |
| Gas | 102,900, paid by the facilitator |
| Payer's native balance | **0 wei, before and after** |

Three things that matter more than the payment itself. The payer never held any native currency,
so it needed nothing but a key and a signature. The transaction was submitted by the facilitator,
so the house was never in the path of the money. And nobody approved either address: no account, no
identity, no registration, on either side.

## Verified in production

On 2026-09-09, against the live house at nyx5.com: a mailbox charging 25 tokens announced
`"amount": "25"` in its `402`, refused an envelope with no stamp, accepted one with a stamp, and
returned a settlement whose transaction id resolves in the ledger to a balanced, house-signed
double entry of 25 out, 20 in and 5 of house fee.

## What is built

| Piece | Where |
|---|---|
| Wire module: build, parse, validate | `src/puentes/x402.js` |
| Capability declaration | `GET /x402/supported` |
| Price of delivery into a mailbox | `GET /x402/inbox/<name>` |
| `402` and settlement on delivery | `POST /inbound` response headers |
| Paying: Keccak-256, secp256k1 with recovery, EIP-3009 signing, the client flow | `src/nucleo/keccak.js`, `src/nucleo/secp256k1.js`, `src/puentes/x402-pagador.js` |
| Tests | `test/x402.test.js`, `test/x402-pagador.test.js` |

Hard rules the tests hold to, because each one is a way to be silently wrong:

- `amount` is a string of atomic units. A number there is money in floating point.
- `network` matches the CAIP-2 grammar.
- `success`, `transaction` and `network` are always present in a settlement, even when the
  transaction id is empty.
- The price announced in the `402` equals the price the ledger charges. If the announcement and
  the ledger drift apart, the adapter is lying, and the test fails.

## Paying

Until 2026-09-14 a Nyx5 agent could only charge. Paying on an EVM network means signing an
EIP-3009 `TransferWithAuthorization` over the USDC contract, typed with EIP-712, and that needs
Keccak-256 and secp256k1 with public-key recovery. Node ships neither (`sha3-256` is FIPS 202,
which pads differently and gives a different hash), and this repository adds no dependencies. So
both are now written here, in plain JavaScript, and checked against public vectors:

| Piece | Checked against | Verdict |
|---|---|---|
| `keccak256` | The Keccak team's `ShortMsgKAT_256.txt` (KeccakKAT-3.zip), entries of 0, 24, 1088 and 2000 bits; the last two cross the 136-byte block boundary | **equivalent** |
| secp256k1 signing | RFC 6979 deterministic nonce; the public vector for private key 1 over sha256("Satoshi Nakamoto") reproduces `r` and `s` exactly | **equivalent** |
| Address derivation | Private keys 1 and 2 give the addresses everybody knows, with the EIP-55 checksum | **equivalent** |
| EIP-712 domain separator | The `Ether Mail` example in EIP-712 itself, and the `DOMAIN_SEPARATOR()` the USDC contract on Ethereum publishes, computed from `TOKEN_USD` | **equivalent** |
| Recovery (`ecrecover`) | Every signature the tests produce recovers to the signer, and a signature made for one network recovers to a different address on every other one | **equivalent** |

The client flow is `pagar({ url, privKey, tope })` in `src/puentes/x402-pagador.js`: `GET` the
resource, read `PAYMENT-REQUIRED`, pick the first `exact` entry on a network in `TOKEN_USD`, sign,
retry with `PAYMENT-SIGNATURE`, return the body and the decoded `PAYMENT-RESPONSE`. What protects
the payer, because a signature is money:

- **`tope` is mandatory**, in atomic units. If the `402` asks for more, nothing is signed. The
  test proves the check runs before the key is even read.
- **The nonce is 32 random bytes per authorization.** The USDC contract refuses a reused nonce, so
  an authorization settles once, whatever the server does with it. The house that charges also
  deduplicates by nonce (`claveDePago`), and the test shows the same header sent twice delivers once.
- **`validBefore` is never later than now + 5 minutes**, even when the server asks for an hour. A
  signed authorization is a bearer cheque until it expires.
- **The EIP-712 domain comes from `TOKEN_USD`, never from the `402`.** A server that announces a
  different `asset`, `name` or `version` for a network gets no signature. Signing against a domain
  the payee dictates is signing whatever the payee wants.
- **`from` must be the key's own address.** Anything else is a signature that cannot validate, with
  an error that would not say why.
- **One signature per call, against the first `402`.** A second `402` (another `payTo`, another
  price, "no funds") is an error, never a second signature. And because the signature has already
  left the process, any error raised after it was sent carries `firmado: true`, `nonce`, `red`,
  `monto`, `destinatario`, `pagador` and `validBefore`: the server may still settle that cheque
  until it expires, and the payer needs the nonce to reconcile. `alFirmar(entregado)` runs before
  the signature is sent, so the record can be written before the step, not after.
- The server's `maxTimeoutSeconds` can only shorten the 5 minutes: a negative, non-numeric or
  fractional value is ignored (a negative one used to produce an already-expired authorization).
- A malformed private key is rejected without quoting it (`deHex` used to echo its first 20
  characters into the error).

The round trip is tested end to end with no chain and no funds: a resource server built only from
`src/puentes/x402.js` (the charging side), a fake facilitator that does what a real one does in
`/verify` and `/settle` except touch the chain (it recomputes the digest, recovers the payer and
compares it with `from`), and the payer above. The recovered payer, not the claimed one, is what
ends up in the settlement.

`demo/x402-pagar.mjs` runs it against a real URL: the key is read from a file named on the command
line and never printed, `--tope` is required, and the only output is the network, the amount, the
payee and the transaction hash from `PAYMENT-RESPONSE`. It moves real money if the key is funded,
so it does not run without explicit authorization.

**Recording it in the ledger.** A payment on a chain is not a ledger entry: no tokens moved and
the house was never in the path of the money, which is the point. What the payer keeps is the
`PAYMENT-RESPONSE`, and the way to give it a date and a witness is the notary: seal the sha256 of
the settlement JSON with `notarize { sha256, name: "x402 <network> <transaction>", media:
"application/json" }`, and the house signs that this receipt existed no later than then. Anyone
can verify the seal at `GET /notaria/<sha256>` without an account; the transaction hash resolves on
the chain's own explorer. That is the same division of labour as the rest of this document: the
chain moves the money, the ledger records what was agreed and by whom.

**Not done here**: the signing is not constant-time (scalar multiplication over BigInt leaks
timing), which is fine for an agent signing a few payments in its own process and not for a server
signing with a valuable key on demand. No `upto` or `auth-capture` client. No wallet management:
the key is a file the operator provides.

## The binding we cannot file yet, and why

Submitting a scheme binding to `specs/schemes/` would put Nyx5 in the official specification
directory. Writing it surfaced a problem that is worth stating plainly, because it is the kind
this project exists to catch.

The `exact` scheme requires, for the family our method belongs to, that settlement produce exactly
one identifiable transfer of the advertised `amount` to `payTo`. It asks each method to declare a
fee payer that is either sponsored by the facilitator or self-funded by the payer.

A Nyx5 house takes its commission out of what the payee is credited. A payment of 25 posts as
payer −25, payee +20, house +5. So `payTo` does not receive the advertised amount, and the fee
model is neither of the two options `exact` offers. The other candidate, `batch-settlement`, does
not fit either: it stores a commitment and moves value later, and we move value immediately.

We are between the two schemes, and filing a binding that reads past a MUST would be exactly the
free assertion this protocol was built to make expensive. So it is not filed.

**Open with the x402 Foundation** (2026-09-09):

- [Issue #3435](https://github.com/x402-foundation/x402/issues/3435) asks which scheme a
  ledger-backed network with a payee-side commission should bind to, with four candidate answers.
- [PR #3436](https://github.com/x402-foundation/x402/pull/3436) is the separate, self-contained
  fix: §11.1 now states the CAIP-2 grammar and that a dot is not allowed in a reference.

One of the four answers, that `payTo` must receive the full advertised amount, would change how
the ledger posts a commission. That is a decision about how the house earns, not an implementation
detail, so it waits for Nicholas.

Third-party explorers list x402 resources, but they index on-chain networks and it is unverified
whether they accept one that has no chain.

## Sources

- <https://github.com/x402-foundation/x402>, `specs/x402-specification-v2.md`
- `specs/transports-v2/http.md`
- `specs/schemes/batch-settlement/scheme_batch_settlement_cloudflare.md`
- `specs/CONTRIBUTING.md` and `specs/extensions/bazaar.md`
